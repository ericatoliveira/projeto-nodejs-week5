import {venda} from './venda';

describe ('Calculo do valor de venda', () => {
    let valorDesconto, valorLucro;

    beforeAll ( () {
        valorDesconto = 0.1;
        valorLucro = 0.5;
    });

    it ('exibir o valor final')
    expec (venda(1, 20, valorCustoTotal, ValorDesconto, Valor Lucro)). toBe (28);
});